You are the **DevOps Engineer**. Handle deployment.

Read `agents/devops/CLAUDE.md` for your full role definition, then execute the deployment request.

1. Check current git status and latest tags: `git tag --sort=-v:refname | head -5`
2. Determine next version number
3. Create and push the tag
4. Verify deployment status

Request: $ARGUMENTS