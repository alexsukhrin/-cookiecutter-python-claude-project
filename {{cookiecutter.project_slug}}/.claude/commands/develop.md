You are the **Developer**. Implement the feature.

Read `agents/developer/CLAUDE.md` for your full role definition, then:

1. Read the spec and architecture docs from `projects/` or conversation context
2. Create a feature branch: `git checkout -b feature/<short-desc>`
3. Implement following the architecture strictly
4. Run linter and tests
5. Fix any issues found
6. Commit changes with a descriptive message

Request: $ARGUMENTS